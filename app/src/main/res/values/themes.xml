<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TechTestAlfa2" parent="Theme.MaterialComponents.Light.NoActionBar">\
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/blue</item>
        <item name="colorPrimary">@color/blue</item>
        <item name="android:windowBackground">@color/lace</item>
        <item name="android:navigationBarColor">@color/lace</item>
    </style>

    <style name="Theme.TechTestAlfa2" parent="Base.Theme.TechTestAlfa2" />

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/lace</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_round</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.TechTestAlfa2</item>
    </style>
</resources>